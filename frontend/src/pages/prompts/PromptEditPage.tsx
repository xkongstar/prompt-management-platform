import type React from "react"
import { Typography } from "antd"

const { Title } = Typography

const PromptEditPage: React.FC = () => {
  return (
    <div>
      <Title level={2}>Edit Prompt</Title>
      <p>Edit prompt page - Coming soon!</p>
    </div>
  )
}

export default PromptEditPage
